# 3.34.84.136
#db_MADPT_VERSION
# config로 숨기는 방법 찾아오기
spring:
    datasource:
        url: jdbc:mariadb://db-master-service:3306/MadPT
        username: root
        password: 1234
        driver-class-name: org.mariadb.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                #        show_sql : true
                format_sql : true

logging.level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace